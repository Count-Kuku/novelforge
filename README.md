[中文](./README.md) | [English](./README.en.md)

# NovelForge

仓库版本：`v0.7.1`

NovelForge 是一个面向长篇小说和同人创作的本地 LLM 写作工作台。它把项目、故事、资料、设定、检索证据和创作记录持续保存在本地，适合需要长期维护世界观、人物关系和章节进度的创作，而不是一次性聊天生成。

## 主要能力

### 写作与规划

- 一个项目可包含多个独立故事，共享项目资料与知识库，同时隔离故事配置、规划和正文。
- 支持总纲、分卷、剧情段、章节细纲、正文生成、章节审阅和章节评价。
- 自由创作支持续写、重写、候选分支、接受片段、提炼设定和汇编章节。
- 创作配置可控制任务性质、篇幅、工作流深度、资料参考强度和资料版本。
- 全局、项目、故事三层生成规则与提示词选项。
- 项目/故事/章节/单次运行创作提醒（临时要求），以及可审计的生成上下文快照。

### 资料与知识

- 粘贴资料、手动资料卡和长篇 TXT/Markdown 导入。
- 长篇文本按章节标题或长度切分，并保存批次与片段级检查点。
- 通用、角色、关系、时间线、设定、文风、严格原作和同人参考等提取模式。
- 多专家提取计划、待审核设定、自动审核策略、冲突/重复检查和批量回退。
- 正式知识、角色/设定资料卡、别名组和资料包报告。
- 资料来源台账可从知识追溯到资料、分段和证据位置。

### RAG

- 词法、语义和混合检索；Embedding 不可用时仍可使用词法检索。
- 故事、资料版本、来源类型和来源可信度过滤/加权。
- 中文短语增强、实体别名扩展、用户反馈、冲突裁决和结果多样化。
- 检索健康检查、调试视图、来源使用报告和上下文证据说明。
- 固定评测用例与 Recall@K、MRR、nDCG@K、零召回统计。
- 增量向量重建：正常情况下复用未变化向量；不可复用的异常向量只重新生成对应片段，模型或向量维度变化时安全切换为完整重建。

### 持久资料任务

长篇资料自动处理会创建 SQLite 任务并立即返回界面：

- 应用进程内后台执行，关闭或刷新浏览器页面不会停止任务。
- 多窗口/多实例通过原子领取、worker 租约和心跳避免重复处理。
- 任务创建与批次保存/删除在同一数据库写锁下判定；界面旧快照、跨任务写入和失租 worker 会被拒绝。
- 应用异常退出后，重新启动会在旧租约过期后接管任务。
- 支持暂停、继续、取消、只重试失败片段和已完成片段跳过。
- 创建前估算模型调用数、输入/输出 Token、Embedding Token 和费用。
- 支持状态筛选、归档、恢复归档、永久删除和过期归档清理。

这里的“后台”不等于独立系统服务：如果退出 NovelForge、结束 Python 进程或关闭电脑，当前模型调用会中断；再次启动应用后会依据数据库检查点恢复。

## 界面导航

- `工作台`：项目总览、项目资源。
- `资料`：资料导入、核心设定、检索中心。
- `规划`：创作配置，以及当前故事需要的大纲、分卷、剧情段和细纲。
- `写作`：自由创作、正文生成、章节评价。
- `配置`：模型配置、生成规则、提示词选项。

`资料导入` 内部提供资料任务、资料来源、待审核设定、处理记录、长篇批次、知识整理和资料包等工作区。一般情况下从工作台给出的“推荐下一步”继续即可，不需要理解内部表结构。

## 推荐工作流

### 首次使用

1. 在 `模型配置` 中添加 OpenAI-compatible 服务地址、API Key、聊天模型和可选 Embedding 模型。
2. 测试连接并保存档案。
3. 新建项目和故事，在 `创作配置` 中确定任务类型、篇幅和参考强度。
4. 需要遵守原作或参考资料时，先进入 `资料导入`。

### 资料处理

1. 上传或粘贴 TXT/Markdown，检查切分预览并保存长篇批次。
2. 选择导入索引、知识提取、自动审核等选项。
3. 在执行前检查片段、调用数和 Token/费用估算。
4. 创建后台任务后，在 `资料任务` 查看进度；关闭页面也可以继续处理。
5. 任务结束后处理 `待审核设定`，再到 `检索中心` 查看健康度并运行固定评测。

### 章节创作

1. 讨论并保存大纲或章节方向。
2. 生成章节细纲和正文，或直接在自由创作中迭代片段。
3. 在生成前展开上下文预览，检查规则、核心设定、检索证据和预算遗漏。
4. 审阅或评价章节，把稳定的新事实提炼为待审核知识。
5. 确认知识后继续后续章节，使新设定进入检索与上下文。

## 安装与运行

需要 Python 3 和 `requirements.txt` 中的依赖。

Windows PowerShell：

```powershell
py -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
Copy-Item .env.example .env
.\.venv\Scripts\python.exe -m streamlit run app.py
```

也可以不编辑 `.env`，启动后在 `模型配置` 页面填写并测试连接。

常用环境变量：

```env
LLM_API_KEY=
DEEPSEEK_API_KEY=
LLM_BASE_URL=https://api.deepseek.com
LLM_MODEL=deepseek-chat
LLM_EMBEDDING_MODEL=

# 仅用于资料任务本地估算，单位：USD / 1M tokens
LLM_INPUT_PRICE_PER_MILLION=0
LLM_OUTPUT_PRICE_PER_MILLION=0
LLM_EMBEDDING_PRICE_PER_MILLION=0
```

价格留空或填 `0` 时，任务仍显示 Token 估算，但不会猜测费用。估算不是供应商实际账单。

`.env` 用于首次启动时引导默认模型方案。模型方案写入 `data/global.db` 后，数据库是后续读取的权威来源；此时请在 `模型配置` 页面修改模型和费率，单独编辑 `.env` 不会覆盖已经保存的方案。

## 数据与备份

- 全局结构化配置保存在 `data/global.db`。
- 每个项目的结构化数据保存在 `data/projects/{project_name}/project.db`。
- 章节、大纲、审阅、分析和导入原文等长文本继续保存为 Markdown/TXT 文件，并由数据库登记。
- 结构化 JSON 默认不再写入；旧文件只承担兼容导入职责。

定期备份整个 `data/`。为获得一致备份，建议先停止 NovelForge，再复制目录；`.env` 含有密钥，应单独安全保存，不要提交到版本库。完整存储契约见 [storage_architecture.md](./storage_architecture.md)。

## Windows 便携版构建

便携版包含轻量 `NovelForge.exe` 启动器、自包含 `.runtime` Python、项目源码和本地 `data/` 目录。运行时会在 `127.0.0.1` 启动 Streamlit，优先使用端口 `8501` 并自动打开浏览器。

准备好不含 `pyvenv.cfg` 的自包含 Windows Python runtime 后执行：

```powershell
.\build_release.ps1 -Version v0.7.1 -RuntimeRoot D:\Runtimes\python-standalone
```

`-Version` 必须与 `VERSION` 一致。构建会生成 ZIP、SHA-256 校验文件和本地日志。把便携版解压到可写目录，例如 `D:\Apps\NovelForge\`，不要放入 `C:\Program Files\`。

## 当前限制与后续重点

- 父子级切分、类型专用切分、多路查询和 RRF 排名融合尚未完成，是下一阶段 RAG 重点。
- 稳定导入入口当前以粘贴文本、TXT 和 Markdown 为主，EPUB、DOCX、PDF 和目录导入尚未提供。
- 后台资料任务依赖 NovelForge 应用进程，不是常驻服务或分布式队列。
- 专用向量数据库和 GraphRAG 只会在本地 SQLite/当前检索链路出现实际规模瓶颈后再评估。
- 多 Agent 自治编排仍在规划中，当前使用显式、可恢复、可测试的工作流。

## 开发文档

- [project.md](./project.md)：当前架构、模块职责、开发边界、技术债和优先级。
- [storage_architecture.md](./storage_architecture.md)：DB-first 权威边界、schema v9、迁移、任务租约和恢复。
- [docs/releases](./docs/releases)：已发布版本历史。

已完成的一次性计划不会长期保留；完成结果应合并进上述事实文档，避免出现多个相互矛盾的“当前状态”。

## License

仓库当前未包含 License 文件。
