# v0.7.1 — Vue 双工作台迁移执行记录

日期：2026-08-20（执行收口）

本次执行建立了 Vue 3 + TypeScript + Vite 前端、FastAPI `/api/v1` 本地 API、规划/对话两个独立 Layout，以及故事级 `creation_mode` schema 16 字段。旧 Streamlit UI 仍作为兼容回退；launcher 默认在存在 `frontend/dist` 时启动 FastAPI/Vue。

已验证：故事模式读写与无损切换、对话上下文排除规划资产、API/错误 envelope/本地安全头、mutation 幂等回放、SSE heartbeat/取消/有界重放、前端 operation replay 断线 fixture、OpenAPI drift check、真实会话流入口、规划方向/结构树/章节编辑/context checker、章节讨论/结构冲突校验/版本时间线、对话动作/附件托盘/多文件资料导入/会话归档、共享知识详情/类型化编辑/证据与手动合并/实体投影/内容浏览、规则/提示词/自动配置/多维用量检查器、普通用户原始 JSON 隐藏与开发者投影守卫、知识 10,000 条搜索 p95 179.0ms、schema 15→16 旧库升级与未来版本拒绝、OpenAI-compatible HTTP 半截流夹具、OCR 预览/批量导入 API 与逐页进度 fixture、Vue typecheck、Vitest（5 tests）、16 项 Playwright Chromium + Edge E2E（8 个场景 × 2 浏览器，移动/1440/1280/1024/768 视口、键盘、刷新、reduced-motion、WCAG Axe、组合输入保护、8,000 次长草稿）、生产构建、静态 SPA fallback、真实 launcher 子进程 ready/端口冲突/已有实例/重启状态矩阵、launcher 锁和发布路径保护。最新便携包已完成 PyInstaller、包结构验证，以及包内 `/`、`health/ready`、`bootstrap`、`usage/breakdown` API 冒烟。

当前便携包：`release/NovelForge-windows-portable-v0.7.1.zip`；同目录 `.sha256` 文件由构建脚本生成。

干净 Windows 与真实 provider/物理 IME 的签收步骤见 [v0.7.1-clean-windows-checklist.md](v0.7.1-clean-windows-checklist.md)。

仍需发布环境完成：真实模型长流断线恢复、物理中文 IME、长会话与超大资料交互压力、真实 OCR/provider 评测，以及至少一个明确兼容窗口。`.github/workflows/vue-migration-windows.yml` 已把 clean Windows runner、Edge、旧库、launcher 和协议夹具纳入可重复入口；上述剩余证据完成后再执行 VF-8.5，移除 Streamlit 回退与依赖。
