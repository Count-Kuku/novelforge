<template>
  <main class="preview-page"><p class="eyebrow">DESIGN SYSTEM / PREVIEW</p><h1>NovelForge primitives</h1><p class="muted">用于验收颜色、焦点、禁用、加载和错误状态；生产页面只复用这些语义，不复制业务规则。</p><div class="preview-grid"><article><p class="eyebrow">BUTTONS</p><div class="row"><button class="button accent">主要动作</button><button class="button secondary">次要动作</button><button class="button" disabled>禁用</button></div></article><article><p class="eyebrow">INPUT</p><input class="preview-input" placeholder="键盘 Tab 可聚焦" /><div class="preview-error">错误状态：请检查输入</div></article><article><p class="eyebrow">STATUS</p><div class="row"><span class="pill">规划模式</span><span class="pill loading">同步中…</span></div></article></div></main>
</template>
<style scoped>
.preview-page { max-width: 920px; margin: 80px auto; padding: 0 24px; }.preview-page h1 { margin: 10px 0; font-family: Georgia, serif; font-size: 45px; font-weight: 400; }.preview-page > p:not(.eyebrow) { max-width: 550px; line-height: 1.8; }.preview-grid { display: grid; gap: 16px; margin-top: 45px; }.preview-grid article { padding: 25px; border: 1px solid var(--line); border-radius: 18px; background: var(--paper); }.row { display: flex; align-items: center; flex-wrap: wrap; gap: 10px; }.preview-input { width: 100%; max-width: 360px; padding: 12px; border: 1px solid var(--line); border-radius: 10px; outline: none; }.preview-input:focus { border-color: var(--accent); box-shadow: 0 0 0 3px var(--accent-soft); }.preview-error { margin-top: 9px; color: #b55f46; font-size: 12px; }.loading::before { width: 7px; height: 7px; border: 1px solid currentColor; border-top-color: transparent; border-radius: 50%; animation: spin .8s linear infinite; content: ''; } @keyframes spin { to { transform: rotate(360deg); } }
</style>
