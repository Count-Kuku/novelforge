# NovelForge 全面审查与修复说明

审查日期：2026-07-16

## 一句话结论

本轮已经把项目中容易导致数据丢失、故事之间数据串线、复制或删除不完整、检索结果过期，以及启动和发布不稳定的问题集中修复，并补齐了自动验证。

## 修复了什么

### 1. 数据安全

- 数据库升级到 schema v6，并加强升级失败时的回滚保护。
- 故事创建、重命名、归档、复制和删除使用事务及写锁，避免并发操作互相覆盖。
- 知识确认、合并、移动和删除改成原子操作，不再出现只完成一半的状态。
- 发现空数据库文件时停止并提示处理，不再自动覆盖原文件。

### 2. 故事和项目隔离

- 工作流、知识、检索、反馈和评测数据现在都会严格绑定所属项目与故事。
- 修复切换项目或故事后，页面仍显示上一个项目状态的问题。
- 拒绝非法路径、Windows 保留名称以及跨故事工作流恢复。

### 3. 故事复制与删除

- “完整复制故事”会同步复制相关知识、素材、工作流和文件，并重写目标路径及标识。
- 复制失败时只清理本次新建的目标故事，不影响其他故事。
- 删除故事时会清理其直接和间接关联数据；数据库操作失败时不会继续误删文件。

### 4. 检索与 AI 工作流

- 检索索引记录内容哈希，正文变化后旧索引会自动失效。
- 语义检索不可用时自动回退到关键词检索。
- 加强 AI 返回内容校验，能识别空结果、错误 JSON、非法数字等异常。
- 章节摘要或知识保存失败时会明确返回失败，不再显示假成功。

### 5. 启动和发布

- 启动器增加跨进程锁、端口竞争保护和异常退出后的状态清理。
- 发布脚本会拒绝把开发虚拟环境误当成便携运行时，也会阻止递归复制目录。
- 中英文 README 和项目设计文档已同步更新。

## 验证结果

- 数据库存储总套件：11 个子套件全部通过。
- 综合回归检查：73 项通过。
- 故事复制与镜像：24 项通过。
- 知识原子操作：14 项通过。
- 待确认知识事务：21 项通过。
- 工作流保护：33 项通过。
- 启动与发布保护：5 项通过。
- Streamlit 页面冒烟测试通过，没有捕获到页面运行异常。
- 全仓 Python 编译、依赖完整性和 Git 补丁格式检查通过。

## 仍需真实环境验证的部分

- 没有调用真实外部大模型接口，相关异常分支使用自动测试验证。
- 没有生成最终便携发行包，因为打包需要额外提供自包含的 `RuntimeRoot`。

除上述两项环境相关验证外，本轮审查发现的问题均已修复并通过本地回归检查。
